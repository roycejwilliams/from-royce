name: Deploy to Firebase

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install root dependencies & build
        run: npm ci && npm run build

      - name: Prepare Firebase Functions directory
        run: |
          cp -r .next functions/
          cp -r public functions/
          cp next.config.js functions/

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Install Functions Dependencies
        working-directory: functions
        run: npm install

      - name: Authenticate with Firebase (if using service account)
        uses: google-github-actions/auth@v1
        with:
          credentials_json: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'

      - name: Deploy to Firebase
        run: firebase deploy --only functions,hosting --project from-royce
